//* ----------------------------------------------------------------------
//* .NET Core & .NET Standard Build Results
//* ส่วนนี้คือไฟล์ที่ถูกสร้างขึ้นเมื่อเรากด Build ไม่ควรนำขึ้น Git
//* ----------------------------------------------------------------------
bin/
obj/

//* ----------------------------------------------------------------------
//* IDE & Editor Specific Files
//* ไฟล์ตั้งค่าส่วนตัวของ Editor (VS Code, Visual Studio)
//* ----------------------------------------------------------------------
.vs/
.vscode/
/*.user
*.suo
*.userosscache
*.sln.docstates

//* ----------------------------------------------------------------------
//* Environment & Secrets (SECURITY CRITICAL)
//* ส่วนนี้สำคัญที่สุด ห้ามนำไฟล์ที่มี Connection String หรือ Password ขึ้นเด็ดขาด
//* ----------------------------------------------------------------------
//! warning bug need to fix: ตรวจสอบให้แน่ใจว่าไม่ได้ commit ไฟล์ที่มี Real Keys หรือ Passwords
appsettings.json
appsettings.Local.json
appsettings.Development.json
.env
local.settings.json

//* ----------------------------------------------------------------------
//* PostgreSQL & Database Related
//* เราจะไม่เก็บไฟล์ Database จริงๆ ไว้ใน Repo
//* ----------------------------------------------------------------------
//? information: หากใช้ Docker แนะนำให้ ignore folder ที่ mount volumes ด้วย
pgdata/
postgres-data/
*.sql.gz

//todo: หากมีการ Generate EF Core Migration Script แบบ Manual อาจจะเลือก ignore หรือ keep ตามตกลงของทีม
# **/Migrations/*.sql 

//* ----------------------------------------------------------------------
//* Operating System Files
//* ไฟล์ขยะจาก Windows และ macOS
//* ----------------------------------------------------------------------
.DS_Store
Thumbs.db

//* ----------------------------------------------------------------------
//* Logs & Crash Dumps
//* ----------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*